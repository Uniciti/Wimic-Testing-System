<div>
  <h2>Добавить тест</h2>
  <form (ngSubmit)="addTest()">
    <div>
      <label for="testType">Тип теста:</label>
      <select id="testType" [(ngModel)]="selectedTestType" name="testType">
        <option *ngFor="let option of testOptions" [value]="option.value">{{ option.label }}</option>
      </select>
    </div>
    <div>
      <label for="bandwidth">Ширина полосы, МГц:</label>
      <select id="bandwidth" [(ngModel)]="selectedBandwidth" name="bandwidth">
        <option *ngFor="let option of bandwidthOptions" [value]="option.value">{{ option.label }}</option>
      </select> 
    </div>
    <div>
      <label for="testType">Частота, МГц:</label>
      <p-inputNumber
              name="testType"
              inputId="testType"
              [(ngModel)]="inputedFrequncy" 
              [placeholder]="'Введите частоту'"
              class="w-full"
              required /> 
    </div>
    <div class="card flex">
      <div class="flex flex-column gap-2">
        <div *ngFor="let option of modulationOptions" class="field-checkbox">
          <p-checkbox 
              [(ngModel)]="selectedModulation"
              [label]="option.label"
              name="modulationTypes"
              [value]="option" />
        </div>
      </div>
    </div>
    <button type="submit">Добавить</button>
  </form>  
   <ul>
    <li *ngFor="let test of massiveTests; let i = index">
      {{ test.testType }} - {{ test.bandwidth }} - 
      {{ test.frequency == "none" ? "Текущая частота" : test.frequency }} - 
      <table>
        <tr *ngFor="let x of test.modulation">
            <td>{{x.label}}</td>
        </tr>
      </table>
      <button (click)="removeTest(i)"><span style="color:#ff1a01;font-size:11pt"> &#x2718; Удалить </span></button>
    </li>
  </ul> 
  <button (click)="acceptTests()"><span style="color:#17ab09;font-size:11pt"> &#x2713; Принять </span></button>
  <button (click)="closeDialog()">Отмена</button>
</div>. Ты можешь красиво это оформить ? а после добавления теста , тест будет отображаться в таблице.